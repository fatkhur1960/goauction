package test

const (
	// AuthorizeUserEndpoint for testing only
	AuthorizeUserEndpoint = "/auth/v1/authorize"
	// UnauthorizeUserEndpoint for testing only
	UnauthorizeUserEndpoint = "/auth/v1/unauthorize"
	// AddProductEndpoint for testing only
	AddProductEndpoint = "/product/v1/add"
	// ListProductEndpoint for testing only
	ListProductEndpoint = "/product/v1/list"
	// DetailProductEndpoint for testing only
	DetailProductEndpoint = "/product/v1/detail/:id"
	// UpdateProductEndpoint for testing only
	UpdateProductEndpoint = "/product/v1/update/:id"
	// DeleteProductEndpoint for testing only
	DeleteProductEndpoint = "/product/v1/delete/:id"
	// BidProductEndpoint for testing only
	BidProductEndpoint = "/product/v1/bid/:id"
	// RegisterUserEndpoint for testing only
	RegisterUserEndpoint = "/user/v1/register"
	// ActivateUserEndpoint for testing only
	ActivateUserEndpoint = "/user/v1/activate"
	// MeInfoEndpoint for testing only
	MeInfoEndpoint = "/user/v1/me/info"
	// UpdateUserInfoEndpoint for testing only
	UpdateUserInfoEndpoint = "/user/v1/me/info"
)